# Majestic Anti-Sliv (Test Demo)

Небольшой тестовый проект для демонстрации системы предупреждений о возможном сливе склада во фракциях (Majestic RP).

В текущей версии:

* Логи берутся из `warehouse_logs.csv`
* Монитор отслеживает превышение лимита предметов за 1 час
* При превышении отправляется Embed-уведомление в Discord через Webhook
* Один алерт на игрока + предмет + склад в сутки (сброс в 00:00 МСК)

---

## 📦 Требования

Python 3.10+

Установить библиотеки:

```bash
pip install requests tzdata
```

### Зачем нужны:

* `requests` — отправка webhook в Discord
* `tzdata` — поддержка таймзон (Europe/Moscow) на Windows

---

## 📁 Структура проекта

```
monitor.py        # основной монитор (отправляет алерты)
feeder.py         # генератор тестовых логов
config.json       # конфигурация лимитов и webhook
warehouse_logs.csv # CSV-лог (создается автоматически)
state.json        # файл состояния (создается автоматически)
```

---

## ▶ Запуск

### 1. Запустить монитор:

```bash
python monitor.py
```

### 2. В другом окне запустить генератор логов:

```bash
python feeder.py
```

Выбери режим:

* `i` — интерактивный ввод
* `r` — случайная генерация

Пример интерактивного ввода:

```
Nick 12345 TAKE 1 10 Marabunta
Nick 12345 TAKE 2 15 Marabunta
```

Если лимит 20 — при втором TAKE сработает алерт.

---

## 🧠 Логика работы

* Учитываются только действия `TAKE`
* Суммируется количество одинакового предмета за 1 час
* Если превышен лимит — отправляется Discord Embed
* Один алерт на игрока + предмет + склад в день
* Сброс в 00:00 МСК

---

## 🚀 Дальнейшее развитие

После тестирования:

* Перевод с CSV на MS SQL
* Добавление анализа возврата (PUT)
* Учет процента невозврата
* Логирование в БД
* Запуск как службы

---

Проект предназначен для демонстрации идеи и базовой логики обнаружения слива склада.
